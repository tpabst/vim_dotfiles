/*
 * =====================================================================================
 *
 *       Filename:  vimcolorexmple.cpp
 *
 *    Description: Vim exemple to test colorscheme 
 *
 *        Version:  1.0
 *        Created:  02/14/2014 01:45:37 PM
 *       Compiler:  gcc
 *
 *         Author:  YOUR NAME (), 
 *   Organization:  
 *
 * =====================================================================================
 */

#include <stdio.h>
#include <stdlib.h>

#define MAX_INT 12

#ifndef

typedef struct foo foo;
struct foo {
    int a=0;
    int b=8;
}

class bar {
    string name = "thomas";
    int age = 29;
}

using namespace std;

int main(int argc; char* argv[])
{
    try {
        // TODO: do things
        // FIXME: fix things
        // XXX: whatching some movies
        QString("CVisuComUtilsFichier::Constructeur2 - DEBUT - szMode=[%1]")
        
        int a = 18;
        int a = 18e14;
        const int bRetour = true;
        unsigned int *ptr = nullptr;
        static char p = "a";

        friend

    }
    catch (exception)
    {
        switch ( var )
        {
            case 1:
                b=12;
                break;

            default:
                exit(0);
        }

        for (auto i : list)
        {
            foo();
            bar();
            char *c = malloc (12 * sizeof(char));
            c = NULL;
            unsigned const int x = 5;
            int y = 3.2;
            int z = x + y % 2;
            bool bRetour = true;
            bool bEntre = false;
        }

        do {
            if (a ==1 and not bRetour)
                goto loop;
            else
            {
                m_fIZone_TIMEOUT
                float fl = new float();
                *fl = 12;
                delete fl:
            }
        } while (a==0)

    }

    loop:
        printf("msg n=%d\t, sizeof %d, float=%f\n", 1, sizeof(int), 2.3e10);

    return EXIT_SUCCESS;
}

#endif
